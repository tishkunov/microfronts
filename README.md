# 🚀 Микрофронтенды с EventBus

Демонстрационный проект микрофронтендов с полноценной системой обмена данными через EventBus.

## 📦 Структура проекта

```
микрофронтссс2/
├── host/              # Host приложение (порт 3000)
│   └── src/shared/    # EventBus и общие модули
├── react-app1/        # React App 1: График (порт 3001)
├── react-app2/        # React App 2: Счетчик (порт 3002)
└── vue-app/           # Vue App: Форма (порт 3003)
```

## 🎯 Что включено

✅ **EventBus** - централизованная система событий  
✅ **3 микрофронтенда** - React (x2) + Vue  
✅ **Реальный обмен данными** между приложениями  
✅ **Типизация TypeScript** для всех событий  
✅ **Standalone режим** для каждого микрофронта  
✅ **Практические примеры** во всех компонентах  

---

## ⚡ Быстрый старт

### 1. Установка зависимостей

```bash
# В каждой папке (host, react-app1, react-app2, vue-app):
npm install
# или
yarn install
```

### 2. Запуск всех приложений

Откройте **4 терминала** и запустите:

```bash
# Терминал 1 - Host
cd host && npm start

# Терминал 2 - React App 1 (График)
cd react-app1 && npm start

# Терминал 3 - React App 2 (Счетчик)
cd react-app2 && npm start

# Терминал 4 - Vue App (Форма)
cd vue-app && npm start
```

### 3. Откройте в браузере

**Главное приложение:** http://localhost:3000

---

## 🎮 Попробуйте сейчас!

### Пример 1: График → Счетчик

1. Откройте http://localhost:3000
2. **Кликните на столбец графика**
3. Посмотрите, как счетчик изменился!

### Пример 2: Форма → Все

1. Заполните форму (имя, email, возраст, сообщение)
2. **Нажмите "Отправить"**
3. График и счетчик получат данные!

### Пример 3: Авто-обновление

1. В счетчике нажмите **"▶️ Авто +1/сек"**
2. Наблюдайте изменения в реальном времени во всех приложениях!

---

## 📚 Документация

### Для пользователей

- **[README_EVENTBUS.md](./README_EVENTBUS.md)** - Краткое руководство и примеры
- **[EXAMPLES.md](./EXAMPLES.md)** - Практические тесты и сценарии

### Для разработчиков

- **[EVENTBUS_GUIDE.md](./EVENTBUS_GUIDE.md)** - Полная документация API

---

## 🔄 Как это работает

### Архитектура

```
┌─────────────────┐
│   Host App      │ ◄─── Экспортирует EventBus
│   (Port 3000)   │
└────────┬────────┘
         │
    ┌────┴────┬────────┬──────────┐
    │         │        │          │
┌───▼───┐ ┌──▼───┐ ┌──▼────┐     
│React 1│ │React 2│ │ Vue   │     
│График │ │Счетчик│ │Форма  │     
│ :3001 │ │ :3002 │ │ :3003 │     
└───────┘ └──────┘ └───────┘     
     │         │         │         
     └─────────┴─────────┘
          EventBus
```

### Пример взаимодействия

```typescript
// React App 1 отправляет событие
eventBus.emit('chart:dataSelected', {
  value: 65,
  label: 'Январь'
});

// React App 2 получает событие
eventBus.on('chart:dataSelected', (data) => {
  setCount(data.value); // Счетчик = 65
});

// Vue App тоже получает
eventBus.on('chart:dataSelected', (data) => {
  formData.age = data.value; // Возраст = 65
});
```

---

## 📊 События между микрофронтами

### График (React App 1)

**Отправляет:**
- `chart:dataSelected` - выбор данных
- `chart:filterChanged` - смена фильтра

**Получает:**
- `counter:changed` - изменение счетчика
- `form:submitted` - данные формы

### Счетчик (React App 2)

**Отправляет:**
- `counter:changed` - изменение значения
- `counter:reset` - сброс

**Получает:**
- `chart:dataSelected` - данные графика
- `form:submitted` - данные формы

### Форма (Vue App)

**Отправляет:**
- `form:submitted` - отправка формы
- `form:fieldChanged` - изменение поля
- `form:validationError` - ошибки

**Получает:**
- `counter:changed` - счетчик
- `chart:dataSelected` - график

### Общие события

- `data:refresh` - запрос обновления всех данных
- `notification:show` - показ уведомлений
- `microfrontend:loaded` - микрофронт загружен

---

## 🛠️ Технологии

- **Module Federation** (Webpack 5) - для микрофронтов
- **React 18** - для React App 1 и 2
- **Vue 3** - для Vue App
- **TypeScript** - типизация
- **EventBus** - паттерн Observer

---

## 🎯 Особенности

### ✨ Типизированные события

```typescript
interface EventMap {
  'counter:changed': { value: number; source: string };
  'form:submitted': { formId: string; data: any; isValid: boolean };
  // ... и другие
}
```

TypeScript проверяет правильность данных!

### 🔌 Standalone режим

Каждый микрофронт работает независимо:
- http://localhost:3001 - только график
- http://localhost:3002 - только счетчик
- http://localhost:3003 - только форма

### 📝 Логирование событий

Каждый компонент показывает:
- ✅ Статус подключения EventBus
- 📝 Лог последних 10 событий
- 📊 Данные от других микрофронтов

### 🎨 Визуальная обратная связь

- 🟢 EventBus подключен - все работает
- 🔴 EventBus не подключен - standalone режим
- 📥 Входящие события
- 📤 Исходящие события

---

## 🧪 Тестирование

### Автоматические тесты

```bash
# В каждом микрофронте
npm test
```

### Ручное тестирование

Смотрите [EXAMPLES.md](./EXAMPLES.md) для подробных сценариев.

### Тестирование в консоли

```javascript
// Отправить событие
eventBus.emit('counter:changed', { value: 42, source: 'test' });

// Подписаться на событие
eventBus.on('counter:changed', (data) => {
  console.log('Counter:', data.value);
});
```

---

## 🐛 Troubleshooting

### EventBus не подключается

1. Проверьте, что host запущен на порту 3000
2. Проверьте консоль на ошибки
3. Перезапустите все приложения

### События не доходят

1. Проверьте статус (должен быть 🟢)
2. Откройте консоль браузера
3. Проверьте имена событий

### Порты заняты

Измените порты в `webpack.config.js`:

```javascript
devServer: {
  port: 3001, // Измените на свободный порт
}
```

---

## 📖 Обучение

### Новичкам

1. Начните с [README_EVENTBUS.md](./README_EVENTBUS.md)
2. Запустите приложения
3. Попробуйте примеры из [EXAMPLES.md](./EXAMPLES.md)

### Опытным разработчикам

1. Изучите [EVENTBUS_GUIDE.md](./EVENTBUS_GUIDE.md)
2. Посмотрите код компонентов
3. Добавьте свои события

---

## 🚀 Развертывание

### Vercel (рекомендуется)

📖 **Подробное руководство**: См. [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md)

**Краткая инструкция:**

1. **Монорепозиторий** (рекомендуется): Один репозиторий, 4 проекта в Vercel
   - Создайте 4 проекта в Vercel из одного репозитория
   - Укажите Root Directory для каждого: `host`, `react-app1`, `react-app2`, `vue-app`
   - Настройте Environment Variables с URL микрофронтендов

2. **Отдельные репозитории**: 4 репозитория, каждый деплоится отдельно

**Быстрый старт:**

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой каждого микрофронтенда
cd host && vercel --prod
cd ../react-app1 && vercel --prod
cd ../react-app2 && vercel --prod
cd ../vue-app && vercel --prod
```

⚠️ **Не забудьте** настроить Environment Variables с URL задеплоенных приложений!

---

## 🎓 Изучите больше

- [Module Federation](https://webpack.js.org/concepts/module-federation/)
- [Event-Driven Architecture](https://en.wikipedia.org/wiki/Event-driven_architecture)
- [Micro Frontends](https://micro-frontends.org/)

---

## 📝 Лицензия

MIT

---

## 🤝 Вклад

Pull requests приветствуются!

---

## 📞 Поддержка

Если у вас вопросы, создайте Issue в репозитории.

---

**Готово! Начните с [быстрого старта](#-быстрый-старт) выше! 🎉**

---

## 📸 Скриншоты

### Главное приложение
```
┌─────────────────────────────────────┐
│  📊 График (React App 1)            │
│  [Янв] [Фев] [Мар] [Апр] [Май]     │
│   65    59    80    81    56        │
├─────────────────────────────────────┤
│  🔢 Счетчик (React App 2)           │
│         80                          │
│  [➖] [➕] [🔄] [▶️]                │
├─────────────────────────────────────┤
│  📝 Форма (Vue App)                 │
│  Имя: [________]                    │
│  Email: [________]                  │
│  Возраст: [80]  ← синхронизирован!  │
│  [📤 Отправить]                     │
└─────────────────────────────────────┘
```

---

**🎯 Главная цель проекта:** Показать, как микрофронтенды могут легко обмениваться данными через EventBus без прямых зависимостей!

**🚀 Запустите и попробуйте прямо сейчас!**


